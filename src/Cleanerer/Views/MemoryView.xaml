<UserControl x:Class="Cleanerer.Views.MemoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Cleanerer.ViewModels"
             xmlns:conv="clr-namespace:Cleanerer.Converters"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="840"
             d:DataContext="{d:DesignInstance Type=vm:MemoryViewModel}"
             Background="{StaticResource Brush.Bg}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:GaugeLevelToBrushConverter x:Key="GaugeLevelToBrushConverter"/>
    </UserControl.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===================== Header (from Unit 1) ===================== -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="SYSTEM STATUS" Style="{StaticResource Style.Text.Kicker}"/>
            <TextBlock Text="Your memory" Style="{StaticResource Style.Text.PageTitle}" Margin="0,6,0,10"/>
            <TextBlock Text="Live usage, session stats and one-click cleanup."
                       Style="{StaticResource Style.Text.Body}" Margin="0,0,0,24"/>
        </StackPanel>

        <!-- ===================== Usage gauge card ===================== -->
        <Border Grid.Row="1"
                Style="{StaticResource Style.Card}"
                Padding="24"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Left: huge load percent -->
                <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,32,0">
                    <TextBlock Text="{Binding LoadPercent, StringFormat={}{0}%}"
                               FontFamily="{StaticResource Font.Default}"
                               FontSize="44" FontWeight="Bold"
                               Foreground="{StaticResource Brush.TextPrimary}"/>
                    <TextBlock Text="memory load"
                               Style="{StaticResource Style.Text.Body}" FontSize="12" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Right: used/total line + slim gradient gauge bar -->
                <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="{Binding UsedFormatted}" FontWeight="Bold" FontSize="15"
                                   Foreground="{StaticResource Brush.TextPrimary}"/>
                        <TextBlock Text=" / " FontWeight="Bold" FontSize="15"
                                   Foreground="{StaticResource Brush.TextMuted}"/>
                        <TextBlock Text="{Binding TotalFormatted}" FontWeight="Bold" FontSize="15"
                                   Foreground="{StaticResource Brush.TextPrimary}"/>
                    </StackPanel>
                    <ProgressBar Style="{StaticResource Style.ProgressBar.Slim}"
                                 Height="12"
                                 Minimum="0" Maximum="100"
                                 Value="{Binding LoadPercent, Mode=OneWay}"
                                 Foreground="{Binding GaugeLevel, Converter={StaticResource GaugeLevelToBrushConverter}}"/>
                </StackPanel>

                <!-- Below: two muted stat rows -->
                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,22,0,0">
                    <WrapPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal" Margin="0,0,24,4">
                            <TextBlock Text="Pagefile: " Style="{StaticResource Style.Text.Body}" FontSize="12"/>
                            <TextBlock Text="{Binding PagefileLine}" FontSize="12" FontWeight="SemiBold"
                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,24,4">
                            <TextBlock Text="Virtual: " Style="{StaticResource Style.Text.Body}" FontSize="12"/>
                            <TextBlock Text="{Binding VirtualLine}" FontSize="12" FontWeight="SemiBold"
                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="System cache: " Style="{StaticResource Style.Text.Body}" FontSize="12"/>
                            <TextBlock Text="{Binding SystemCacheLine}" FontSize="12" FontWeight="SemiBold"
                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                        </StackPanel>
                    </WrapPanel>
                    <WrapPanel Orientation="Horizontal">
                        <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                            <TextBlock Text="Avg: " Style="{StaticResource Style.Text.Body}" FontSize="12"/>
                            <TextBlock Text="{Binding AvgLine}" FontSize="12" FontWeight="SemiBold"
                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                            <TextBlock Text="Max: " Style="{StaticResource Style.Text.Body}" FontSize="12"/>
                            <TextBlock Text="{Binding MaxLine}" FontSize="12" FontWeight="SemiBold"
                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Min: " Style="{StaticResource Style.Text.Body}" FontSize="12"/>
                            <TextBlock Text="{Binding MinLine}" FontSize="12" FontWeight="SemiBold"
                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                        </StackPanel>
                    </WrapPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!--
            Two columns: Tasks on the left, Results on the right.
        -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ===================== Tasks card ===================== -->
            <Border Grid.Column="0"
                    Style="{StaticResource Style.Card}"
                    Padding="20"
                    Margin="0,0,8,0"
                    VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Tasks" FontFamily="{StaticResource Font.Default}"
                               FontSize="15" FontWeight="Bold"
                               Foreground="{StaticResource Brush.TextPrimary}"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="One-click cleanup actions."
                               Style="{StaticResource Style.Text.Body}" Margin="0,0,0,16"/>

                    <Button Style="{StaticResource Style.Button.GhostRow}"
                            Command="{Binding TrimCommand}"
                            IsEnabled="{Binding IsNotBusy}"
                            Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Trim working sets" FontWeight="SemiBold" FontSize="13"
                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                            <TextBlock Text="Force every process to release unused physical memory."
                                       Style="{StaticResource Style.Text.Body}" FontSize="11" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource Style.Button.GhostRow}"
                            Command="{Binding ClearCacheCommand}"
                            IsEnabled="{Binding IsNotBusy}"
                            Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Clear system cache" FontWeight="SemiBold" FontSize="13"
                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                            <TextBlock Text="Flush the file cache and purge standby memory. Needs admin."
                                       Style="{StaticResource Style.Text.Body}" FontSize="11" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource Style.Button.GhostRow}"
                            Command="{Binding ClearClipboardCommand}"
                            IsEnabled="{Binding IsNotBusy}"
                            Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Clear clipboard" FontWeight="SemiBold" FontSize="13"
                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                            <TextBlock Text="Wipe whatever is currently held on the clipboard."
                                       Style="{StaticResource Style.Text.Body}" FontSize="11" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource Style.Button.GhostRow}"
                            Command="{Binding RestartExplorerCommand}"
                            IsEnabled="{Binding IsNotBusy}">
                        <StackPanel>
                            <TextBlock Text="Restart Explorer" FontWeight="SemiBold" FontSize="13"
                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                            <TextBlock Text="Restart the Windows shell to release leaked UI memory."
                                       Style="{StaticResource Style.Text.Body}" FontSize="11" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <!-- ===================== Results card ===================== -->
            <Border Grid.Column="1"
                    Style="{StaticResource Style.Card}"
                    Padding="20"
                    Margin="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,16">
                        <TextBlock Text="Results" FontFamily="{StaticResource Font.Default}"
                                   FontSize="15" FontWeight="Bold"
                                   Foreground="{StaticResource Brush.TextPrimary}"/>
                        <TextBlock Text="Newest first."
                                   Style="{StaticResource Style.Text.Body}" Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Empty state: dashed accent border -->
                    <Grid Grid.Row="1"
                          Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}"
                          VerticalAlignment="Top" Margin="0,4,0,0">
                        <Rectangle Stroke="{StaticResource Brush.Accent}" StrokeThickness="1"
                                   StrokeDashArray="4 3" RadiusX="14" RadiusY="14" Opacity="0.55"/>
                        <TextBlock Text="Run a task to see results here."
                                   Style="{StaticResource Style.Text.Body}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="24"/>
                    </Grid>

                    <!-- Populated state: checklist rows -->
                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  Visibility="{Binding HasResults, Converter={StaticResource BoolToVis}}">
                        <ItemsControl ItemsSource="{Binding Results}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:TaskResult}">
                                    <Grid Margin="0,0,0,14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" FontSize="14" FontWeight="Bold"
                                                   VerticalAlignment="Top">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="&#x2713;"/>
                                                    <Setter Property="Foreground" Value="{StaticResource Brush.Success}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Ok}" Value="False">
                                                            <Setter Property="Text" Value="&#x2717;"/>
                                                            <Setter Property="Foreground" Value="{StaticResource Brush.Danger}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="13"
                                                       Foreground="{StaticResource Brush.TextPrimary}"/>
                                            <TextBlock Text="{Binding Detail}"
                                                       Style="{StaticResource Style.Text.Body}"
                                                       FontSize="12" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
